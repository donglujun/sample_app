<%= form_for(@employee) do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control', placeholder: 'Text', type: 'text' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-default' %>
  </div>
<% end %>